services:
  server:
    build: ./server
    restart: unless-stopped
    env_file: .env
    ports: ["5000:5000"]
    volumes: [./uploads:/app/uploads]
  client:
    build: ./client
    restart: unless-stopped
    ports: ["8080:80"]
    depends_on: [server]
