import {useState} from 'react';import {Link,useLocation,useNavigate} from 'react-router-dom';import {useForm} from 'react-hook-form';import {HardHat,Mail,Lock,ArrowRight} from 'lucide-react';import {useAuth} from '../../context/AuthContext';import {messageOf} from '../../api/client';
export default function LoginPage(){const {login}=useAuth(),navigate=useNavigate(),location=useLocation(),[serverError,setServerError]=useState('');const {register,handleSubmit,formState:{errors,isSubmitting}}=useForm();const submit=async v=>{try{setServerError('');await login(v);navigate(location.state?.from?.pathname||'/dashboard',{replace:true})}catch(e){setServerError(messageOf(e))}};return <div className="grid min-h-screen lg:grid-cols-2"><section className="hidden bg-navy-950 p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="flex items-center gap-3"><div className="rounded-xl bg-brand-500 p-3"><HardHat/></div><div><div className="font-bold">RT INQUIRY</div><div className="text-xs text-blue-200">PROPOSAL MANAGEMENT SYSTEM</div></div></div><div><div className="mb-6 h-1 w-16 rounded bg-brand-500"/><h1 className="max-w-xl text-4xl font-bold leading-tight">Engineering proposals,<br/>controlled from inquiry to award.</h1><p className="mt-5 max-w-lg text-lg leading-8 text-blue-100">One governed workspace for Sales, Estimation, Design and General Management.</p></div><p className="text-sm text-blue-300">Secure enterprise access • Revision controlled • Fully auditable</p></section><section className="flex items-center justify-center bg-white p-6"><div className="w-full max-w-md"><div className="mb-10 flex items-center gap-3 lg:hidden"><div className="rounded-xl bg-navy-900 p-3 text-white"><HardHat/></div><b>RT Inquiry</b></div><p className="text-sm font-semibold uppercase tracking-widest text-brand-600">Welcome back</p><h2 className="mt-2 text-3xl font-bold text-navy-900">Sign in to your workspace</h2><p className="mt-2 text-sm text-slate-500">Use your company account to continue.</p>{serverError&&<div role="alert" className="mt-6 rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-700">{serverError}</div>}<form onSubmit={handleSubmit(submit)} className="mt-8 space-y-5"><label className="block"><span className="label">Email address</span><div className="relative"><Mail className="absolute left-3 top-3 text-slate-400" size={18}/><input className="field pl-10" type="email" autoComplete="email" placeholder="name@company.com" {...register('email',{required:'Email is required'})}/></div>{errors.email&&<small className="text-red-600">{errors.email.message}</small>}</label><label className="block"><span className="label">Password</span><div className="relative"><Lock className="absolute left-3 top-3 text-slate-400" size={18}/><input className="field pl-10" type="password" autoComplete="current-password" {...register('password',{required:'Password is required',minLength:{value:8,message:'Use at least 8 characters'}})}/></div>{errors.password&&<small className="text-red-600">{errors.password.message}</small>}</label><div className="text-right"><Link className="text-sm font-semibold text-brand-700 hover:underline" to="/forgot-password">Forgot password?</Link></div><button className="btn-primary w-full" disabled={isSubmitting}>{isSubmitting?'Signing in…':<>Sign in <ArrowRight size={18}/></>}</button></form></div></section></div>}
